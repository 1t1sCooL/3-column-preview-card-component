apiVersion: apps/v1
kind: Deployment
metadata:
  name: 3-column-preview-card-component-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 3-column-preview-card-component-app
  template:
    metadata:
      labels:
        app: 3-column-preview-card-component-app
    spec:
      containers:
        - name: 3-column-preview-card-component-app
          image: 3-column-preview-card-component-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
