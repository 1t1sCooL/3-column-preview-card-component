apiVersion: v1
kind: Service
metadata:
  name: three-column-preview-card-component
  namespace: default
spec:
  selector:
    app: three-column-preview-card-component-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
